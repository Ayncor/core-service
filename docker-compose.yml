services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ayncor
      POSTGRES_PASSWORD: ayncor
      POSTGRES_DB: core_service
    ports:
      - "54322:5432"
    volumes:
      - core_service_pg_data:/var/lib/postgresql/data

volumes:
  core_service_pg_data:

